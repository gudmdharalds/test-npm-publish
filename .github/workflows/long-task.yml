---
name: Long task
on:
  push:
  workflow_dispatch:

jobs:
  long_task:
    name: Long task
    runs-on: ubuntu-latest
    steps:    
      - name: Check out the source code
        uses: actions/checkout@v3
     
      - name: Run unit tests
        run: sleep 10
        env:
          TESTING_DEBUG_MODE: 'true'
